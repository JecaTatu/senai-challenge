<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="/views/detail/detail.css">

  <!-- import google maps -->
  <link rel="stylesheet" href="/maps/documentation/javascript/cgc/demos.css">

  <title>
    <%= user.name.first + " " + user.name.last %>
  </title>
</head>

<body>
  <div id="profile">
    <div id="photo">
      <img src="<%= user.picture.large %>" alt="imagem do usuário">
    </div>
    <div id="info">
      <h2>
        <%= user.name.first + " " + user.name.last %>
      </h2>
      <p>
        <%= user.email %>
      </p>
      <p>
        <%= user.phone %>
      </p>
      <p>
        <%= user.dob.date %>
      </p>
      <address>
        <%= user.location.street + ", " + user.location.city + ", "
         + user.location.state  %>
      </address>

    </div>

    <input type="hidden" lat="<%= user.location.coordinates.latitude %>" lng="<%= user.location.coordinates.longitude %>"
      id="loc">

  </div>

  <div id="map"></div>
  <script src="/views/detail/detail.js"></script>
  <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAalLOtNr-Gmdf-V2Po_kU-5aC6vQwB5aE &callback=initMap"
    async defer></script>

</body>

</html>